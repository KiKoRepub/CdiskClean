# CdiskClean version3 功能实现文档

编写日期：2026-09-20  
代码基线：`main`，提交 `8b6924e`  
文档性质：面向开发的实现方案，不代表下述新增功能已经完成。

## 1. 目标与版本边界

version3 的目标是回答“空间被什么占用、可能由谁产生、删除可能有什么影响”，在现有监测、扫描和清理能力之上补齐解释与历史展示。

范围依据为本地 `docs/plan/plan.txt`。该规划比早期总结更新：自然语言查询与方案生成已移到 version5 / P2，不纳入 version3。本文放在仓库根目录，因为当前 `.gitignore` 忽略整个 `docs/` 目录，便于随代码管理。

以下优先级为本文建议的开发顺序，不是现有功能的完成状态。

| 功能 | 当前基础 | version3 交付内容 | 优先级 |
| --- | --- | --- | --- |
| 监测历史可视化 | 已保存文件变更记录 | 按时间、类型、来源进程统计，图表联动明细 | P0 |
| 清理历史可视化 | 已保存逐项清理记录 | 成功/失败、方式、分类及已知处理量统计 | P0 |
| 分析历史可视化 | 扫描结果仅在内存中 | 保存目录级容量采样，展示同目录历史趋势 | P0 |
| 来源反推 | FSW + ETW 实时归因、历史路径查询 | 汇总历史关联与路径规则，展示证据和不确定性 | P0 |
| 用途与清理风险分析 | 六类分类、路径风险、权限分析 | 解释可能用途、删除影响、风险原因 | P0 |
| 清理建议 | `CleanupCandidate.Recommendation` 已有固定文案 | 根据分析结果形成可解释建议并带入清理中心 | P0 |
| 按应用分组忽略进程 | 当前按进程名管理忽略项 | 用户维护应用组，批量管理成员进程 | P1 |
| AI 辅助解释 | `AgentRequestHelper` 为空类 | 按需解释用途、来源、清理原因和增长现象 | P1 |

version4 的保护清单、完整清理预演、持久化批次及恢复、目录树快照对比、组合筛选不在本期实现；version5 的定时任务、自动清理、迁移管理和自然语言操作亦不纳入。version3 保存目录根级采样，不提前建设全量文件快照系统。

## 2. 现有实现与复用位置

| 层次 | 现有文件 | 本期调整方向 |
| --- | --- | --- |
| 工作台 | `Forms/Form1.Workspace.Dashboard.cs`、`Services/DashboardQueryService.cs` | 历史图表、时间范围和数据覆盖提示 |
| 实时活动 | `Forms/Form1.Workspace.Activity.cs` | 查看来源证据，按图表条件定位活动 |
| 监控规则 | `Forms/Form1.Workspace.Rules.cs` | 应用分组及组内进程管理 |
| 空间分析 | `Forms/Form1.Workspace.Analyzer.cs` | 用途、来源、风险、采样历史和 AI 解释 |
| 清理中心 | `Forms/Form1.Workspace.Cleanup.cs` | 显示建议依据，继续使用现有选择及确认流程 |
| 记录中心 | `Forms/Form1.Workspace.Records.cs` | 接收图表筛选条件，展示对应历史 |
| 监测采集 | `Services/Monitoring/DiskMonitorService.cs`、`EtwMonitorService.cs` | 保持原始归因，必要时补充匹配方式标记 |
| 扫描 | `Services/Analysis/FolderSizeAnalyzer.cs` | 补全扫描完整性，再保存有效采样 |
| 分类及风险 | `Services/Cleanup/CleanupClassifier.cs`、`Helpers/RiskLevelHelper.cs` | 复用规则，补充命中依据及影响描述 |
| 清理执行 | `Services/Cleanup/CleanupService.cs`、`CleanupFileOperations.cs` | 所有建议仍通过既有执行入口和安全校验 |
| 数据持久化 | `Services/database/DatabaseSchema.cs`、`SqliteDatabaseService.cs`、`HistoryStore.cs`、`MonitoringRuleStore.cs` | 增量迁移、历史聚合、采样和分组读写 |
| AI 请求 | `agents/AgentRequestHelper.cs` | 落地一个具体请求实现，不引入 Agent 框架 |

现有 `DashboardQueryService.GetSnapshot()` 只读取最近 5000 条变更、5000 条清理及 500 条通知，不能直接作为完整时间区间统计。`GetChangeRecordsUnderPath()` 默认读取 100 条、最多 5000 条，反推若使用该结果，必须注明是有限样本。

现有 `ChangeRecords` 没有修改前大小，也没有可靠的可执行文件身份；`CleanupRecords` 中目录大小写为 NULL。不能据此计算精确净增长、应用独占容量或完整释放空间。

## 3. 最小实现结构

维持窗体调用具体服务的现有结构，不新增接口层、插件平台或通用工作流。建议仅按职责增加：

- `Services/Analysis/FolderInsightService.cs`：组合目录扫描、来源证据、用途及风险结果。
- `Models/FolderInsight.cs`：保存结构化分析结果及证据条目；小型相关类型可放在同一文件。
- `Models/AnalysisSample.cs`：保存目录根级容量采样。

历史聚合继续放入 `HistoryStore`，工作台由 `DashboardQueryService` 组织展示数据；应用组读写继续放入 `MonitoringRuleStore`。AI 请求使用已有 `AgentRequestHelper`，等实际功能需要时再增加文件。

主流程：

```text
用户选择目录
  → FolderSizeAnalyzer 扫描 + 权限分析
  → 校验取消状态、请求版本及扫描完整性
  → 保存目录根级采样
  → FolderInsightService 汇总历史证据、用途与风险
  → 展示结论、依据、限制
      → 用户请求 AI 解释 → 预览发送摘要 → 展示辅助解释
      → 用户带入清理 → 重新扫描 → 选择与确认
          → CleanupService.ExecuteAsync → CleanupFileOperations
```

采样写入失败时保留本次扫描结果并提示“历史保存失败”；分析、图表或 AI 请求失败均不得阻塞基础清理能力。

## 4. 历史可视化

### 4.1 查询和展示

默认展示最近 7 天，支持今天、30 天及自定义区间。新增查询在 SQLite 中按范围聚合，不能先取最近 N 条再假定代表整个区间。时间条件采用左闭右开 `[start, end)`，点击图表时将相同条件传给记录中心。

| 图表 | 数据源 | 指标与口径 |
| --- | --- | --- |
| 监测活动趋势 | `ChangeRecords` | 按小时/天统计已记录事件数，可按类型拆分；不等同真实系统操作总数 |
| 来源进程排行 | `ChangeRecords.SourceProcess` | 按已记录事件数排序，空值与未知来源单独展示 |
| 清理结果趋势 | `CleanupRecords` | 成功项数、失败项数，按方式和分类筛选；不是清理批次数 |
| 清理处理量 | `CleanupRecords.SizeBytes` | 仅成功且大小已知项求和，同时展示大小未知项数；不标注为释放空间 |
| 分析容量趋势 | 新增 `AnalysisSamples` | 同一规范化根路径、同一统计范围的容量采样，按采样时间展示 |

历史保留上限已存在：初始化时变更记录裁剪至 50000 条、通知和清理记录各 5000 条。因此页面应展示“基于当前保留记录”，并给出最早可用记录时间。没有记录不代表程序始终在监测；容量采样缺失显示空缺，不插值为 0。

现有时间使用 `DateTime.Now.ToString("O")` 保存。本期旧表按既有本地时间语义查询，新增采样同样明确时间语义；不得一部分按 UTC、一部分按本地日期分桶。跨时区历史若无法可靠转换，须提示口径限制。

先用已有 AntdUI 可用控件或 WinForms 绘图能力实现简单柱状图、折线图，具体控件在实现时核对本地依赖。同步提供可读数值表、图例和键盘可访问的筛选控件，不仅依赖颜色传达成功或失败。

### 4.2 容量采样

采样时机为用户手动完成空间分析，不增加后台定时扫描。每次只保存根路径的一条汇总，避免把父目录和子目录容量相加而重复统计。

`AnalysisSamples` 建议字段：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| Id | INTEGER PRIMARY KEY | 采样标识 |
| RootPath | TEXT NOT NULL | 规范化根路径，查询使用一致的大小写比较规则 |
| ScannedAt | TEXT NOT NULL | 实际采样时间，ISO 8601 |
| SizeBytes | INTEGER NULL | 已读取范围的字节数；完全不可读时为 NULL |
| FileCount | INTEGER NULL | 已读取文件数 |
| AccessStatus | TEXT NOT NULL | 沿用 Accessible、Partial、Denied、Missing、Error |
| IsComplete | INTEGER NOT NULL | 在声明扫描范围内是否完成 |
| InaccessibleCount | INTEGER NOT NULL | 无法读取项数 |
| ScopeVersion | INTEGER NOT NULL | 扫描口径版本，首版固定为 1 |
| ErrorMessage | TEXT NULL | 不完整或失败原因摘要 |

建立 `(RootPath, ScannedAt)` 索引。建议初始保留最近 10000 条，随采样写入按 Id 裁剪；这是本方案建议值，不是现有配置。

现有扫描器在取消时可能直接返回部分结果，一些异常分支直接 `continue`。接入持久化前必须修正完整性传递：取消或已过期结果不保存；访问失败、属性读取失败和递归异常要向父节点传播不完整状态。按既定策略跳过重解析点时注明扫描范围不含链接目标。

部分可读采样可保存，但用特殊标记展示，默认不参与容量差值。差值仅用于同路径、相同 `ScopeVersion`、两次完整扫描；扫描本身不是原子快照，差值标为采样变化，不宣称精确追踪每次写入。完整子目录快照与变化清单留到 version4。

## 5. 来源反推与按应用分组

### 5.1 来源反推

反推回答“哪些进程曾操作这里、哪些应用可能使用这里”，不能把最近写入者直接判定为目录创建者或唯一所有者。

证据优先顺序：

1. 当前文件的历史来源记录：展示来源进程、操作类型、时间及匹配路径。
2. 目录及子目录历史聚合：展示各进程次数、最近操作时间及统计区间。
3. 已监测应用、用户维护的应用组关系：说明来自配置关系，不等同文件归属。
4. 路径段与分类规则：仅形成“可能是缓存/日志/临时数据”等弱推断。

新增目录来源聚合查询，对指定时间范围的保留记录执行分组；若为控制开销使用限量记录，结果必须携带样本上限和截断标志。路径匹配复用现有目录边界及 LIKE 转义逻辑，统一先规范化路径；`C:\App` 不匹配 `C:\App2`，路径中的 `%`、`_` 不作为通配符。

分析结果至少包含 `PossibleSources`、`Evidence`、`Confidence`、`Limitations`。置信等级使用“较强/一般/较弱/未知”，并展示产生该等级的规则，不虚构概率。多进程操作保留多个来源，不强行选定一个应用。

ETW 当前同时维护文件与目录事件缓冲。原始进程归因亦可能来自近邻事件，不能全部标为“已证实”。如果界面需要区分文件精确匹配、目录近邻匹配与未知，应将匹配类型随归因结果传递并保存为可空字段 `AttributionKind`；旧记录标为“历史归因，匹配方式未知”。这需要同步调整事件模型、采集、写入和读取，不能靠显示层猜测。

ETW 未启动、进程已退出、历史记录被裁剪或目录从未被监测时，明确显示证据不足。历史反推不能回填覆盖原始 `SourceProcess`。

### 5.2 忽略进程按应用分组

首期使用用户维护的分组，不自动把同名进程或同厂商程序归为同一应用。新增 `IgnoreProcessGroups(Id, Name)`，在 `IgnoreProcessRecord` 增加可空 `GroupId`；旧记录归入“未分组”。一个进程记录最多属于一个组，避免多组启停冲突。

组开关是成员状态的批量编辑：组全启用、全禁用、混合状态由成员计算，不另存组开关。数据库事务成功后刷新内存列表与 ETW 黑名单；同步失败时提示并重新加载一致状态。删除分组只解除成员分组，不自动删除忽略规则。

当前底层忽略逻辑按进程名生效。同名不同路径的进程仍会共同受到影响，界面必须说明这个限制；本期不宣称已提供按可执行文件路径隔离的忽略能力。

## 6. 用途、风险与清理建议

### 6.1 统一结果

`FolderInsight` 建议包含以下信息：目录路径与分析时间、可能用途、候选来源、证据列表、风险等级与原因、删除影响、建议动作、扫描完整性和限制。证据条目区分历史事实、规则推断和 AI 解释，保留关联路径或时间便于检查。

用途规则优先复用 `CleanupClassifier` 的六类分类和 `CleanupClassificationRule`，补充稳定的规则标识与命中原因，不再维护一套互相冲突的扩展名规则。

| 分类 | 解释示例 | 删除影响与建议 |
| --- | --- | --- |
| 临时文件 | 临时工作数据，也可能包含未完成下载 | 确认任务结束；不能仅凭 `.part` 判定可删 |
| 缓存 | 应用生成的可重建数据 | 可能重新下载或降低首次启动速度，先关闭相关应用 |
| 日志 | 运行、审计或故障排查记录 | 删除后失去对应历史，需要保留时不推荐 |
| 崩溃转储 | 崩溃诊断材料 | 确认不再排查问题后考虑回收站 |
| 安装包/更新残留 | 安装、修复或更新相关数据 | 可能影响修复、卸载和更新，保持高风险提示 |
| 其他 | 无足够规则确定用途 | 明确“用途未知”，不默认建议删除 |

### 6.2 风险合并

风险至少取分类规则、现有路径风险和已识别运行/占用风险中的最高等级。无权限、用途未知或扫描不完整单独列为不确定性，不能因为无法读取而显示低风险；AI 不得降低本地规则得到的风险。

目录包含多类子项时显示分类分布及最高风险，不用目录名称覆盖子项风险。正在使用状态只是当时观察，关闭进程也不保证可以删除；最终仍以执行前校验为准。本功能评估的是清理影响，不给出病毒检测结论。

### 6.3 推荐与执行

推荐项展示“建议理由、可能影响、依据、已知大小”。可选动作为“暂不处理”“关闭应用后再检查”“带入清理中心”，默认优先建议可恢复方式。高风险项保持不默认勾选，未知用途项不进入自动推荐集合。

用户点击“带入清理”后，由清理中心重新扫描得到 `CleanupFileEntry`，继续执行分类、树选择、危险项确认及现有路径/大小/修改时间校验。分析结果和 AI 文本都不能直接构造绕过扫描的执行命令。

处理量不等于空间收益：移入回收站、同卷移动、硬链接不能直接记为释放空间，压缩也不能将输入大小当作实际释放量。本期历史图表仅展示有依据的处理量；批次收益报告留到 version4。

## 7. AI 辅助解释

AI 在本地规则和历史证据之后工作。默认关闭，由用户配置服务地址、模型与凭据后主动请求；具体供应商协议在接入时确定，本文不假定空类已经具备请求能力。

### 7.1 请求边界

输入仅为用户预览过的结构化摘要：路径的脱敏标识、分类占比、扫描时间和完整性、历史进程统计、风险规则以及可比较采样差值。不默认读取或上传文件内容、完整日志、用户名和完整本地路径。用户需要发送具体名称时可显式选择。

凭据不写入源码、SQLite 历史、请求日志或异常消息；首期可由用户配置环境变量，若提供持久化界面再使用 Windows 凭据存储。网络请求使用一个可复用 `HttpClient`，支持取消、超时及响应大小上限，不自动无限重试。云端服务使用 HTTPS。

### 7.2 输出和展示

约定输出包含“用途解释、可能来源、清理影响、增长解释、证据引用、不确定项”。证据引用只能指向本次输入提供的条目，未匹配的引用不能显示为已验证依据。文件名、路径和模型响应均视为不可信文本，不允许其中内容变更指令或执行命令。

模型输出以独立“AI 辅助解释”区域显示，不覆盖本地风险等级或原始事实。若采用 JSON 响应，需验证结构、字段长度和枚举值；格式错误则提示解释不可用，不当作操作参数继续处理。

“空间增长原因”只有在存在同范围可比较采样时才有容量变化依据。进程事件数只能说明活动相关性，不能证明某进程贡献了精确增长字节数；没有采样时说明无法判断增长。

请求失败、超时、限流、缺少配置或取消时保留本地结果。切换目录或重新扫描后，用已有取消令牌与请求版本机制丢弃过期响应。本期不保存 AI 对话历史，不调用清理工具，不实现自然语言命令入口。

## 8. 数据库迁移与查询约束

1. 新表定义集中在 `DatabaseSchema`；旧表新增列沿用 `PRAGMA table_info` 检查后增量添加的方式。新增迁移以事务保证一致性，重复初始化不重复建列。
2. 保留现有六张表及记录；新增采样表和分组表。只有采用精细归因展示时才增加 `AttributionKind`，不为未知未来功能预先增加字段。
3. 旧忽略项的 `GroupId` 默认为 NULL；旧归因等级为未知；升级前没有采样则显示“暂无分析历史”，不由变更大小伪造历史容量。
4. 为时间区间查询增加必要索引，如 `ChangeRecords(Timestamp)`、`CleanupRecords(CleanupTime)`。根据实际查询计划再决定来源和路径复合索引，避免堆叠索引增加监测写入成本。
5. SQL 条件全部参数化。路径规范化、大小写、根路径、尾部分隔符和通配字符须统一处理；应用组成员更新在同一事务中完成。
6. 查询在后台执行，结果回到 UI 线程更新。快速切换时间范围只接收最后一次请求结果，窗体关闭时取消任务。
7. 迁移失败时显示可操作错误，不删除旧库或重建覆盖；历史功能不可用时避免把读取失败展示成“0 次”。

## 9. 分阶段开发任务

| 阶段 | 工作内容 | 完成标准 |
| --- | --- | --- |
| A：数据基础 | 采样模型与迁移、完整性传播、时间范围聚合查询 | 旧库升级可重复执行；取消不入库；不完整结果不产生虚假差值 |
| B：历史图表 | 工作台三类历史、筛选联动、数据覆盖提示 | 图表数值与相同条件的数据库明细一致，NULL 大小不作为 0 |
| C：本地解释 | 来源聚合、用途规则、风险原因、清理建议 | 每项结论可查看依据；未知来源不被强行归属；执行入口保持统一 |
| D：应用分组 | 分组持久化、组内增删、批量启停及 ETW 同步 | 重启恢复一致；混合状态正确；删除组不误删忽略项 |
| E：AI 解释 | 配置、摘要预览、请求与取消、输出验证 | 无 AI 仍可使用本地功能；响应无法触发清理或降低风险 |
| F：发布回归 | 旧功能回归、统计口径核对、README 与截图更新 | 明确记录已完成项和遗留限制，不以本方案代替验收结论 |

A 是 B、C 的数据前置；E 依赖 C 的结构化证据。D 可单独实现。先交付本地可用闭环，再完成 AI 接入。

## 10. 验证与验收用例

自动检查继续使用 `testCases/CdiskClean.ClassifierChecks` 的现有控制台断言模式，在相关功能落地时增加少量关键用例，不新建测试框架。

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| V3-01 | version2 数据库升级，连续初始化两次 | 旧数据保留，无重复列或迁移错误 |
| V3-02 | 时间区间包含午夜边界、成功和失败清理项 | 左闭右开，无重复计数，图表与明细一致 |
| V3-03 | 区间记录超过旧的页面读取上限 | 聚合统计不被最近 N 条静默截断 |
| V3-04 | 历史裁剪、未监测、空数据或查询失败 | 分别展示覆盖限制、无记录或读取失败，不混为完整零值 |
| V3-05 | 目录扫描取消、快速切换路径、子目录异常 | 取消/过期结果不入库，异常传递完整性状态 |
| V3-06 | 完全不可读或部分可读目录 | 未知容量不显示为完整 0 字节，不参与默认增长差值 |
| V3-07 | 同路径两次完整采样、不同范围版本采样 | 前者可计算采样差值，后者不直接比较 |
| V3-08 | `C:\App` 与 `C:\App2`，含 `%`/`_` 的路径 | 不跨目录匹配，不扩大查询范围 |
| V3-09 | 多进程写同目录、ETW 不可用、旧来源记录 | 保留多个候选及限制，不伪造创建者或精确归因 |
| V3-10 | 分组批量启停、组内混合状态、删除组、重启 | 数据库与 ETW 一致，成员保留，同名进程限制可见 |
| V3-11 | 高风险目录内包含缓存、扫描后文件变化 | 缓存分类不降低路径风险，执行前变化复核仍生效 |
| V3-12 | 清理目录大小为 NULL、回收站、移动及链接 | 未知项单列，不将处理量误称释放空间 |
| V3-13 | AI 未配置、超时、取消、非法结构、恶意指令文本 | 基础功能可用，无命令执行，无过期结果覆盖 |
| V3-14 | AI 声称高风险文件“安全可删” | 本地风险与确认流程不变 |
| V3-15 | 提交 AI 请求并检查预览与日志 | 默认无文件内容、完整敏感路径和凭据泄露 |

功能实现后的基础验证命令：

```powershell
dotnet build CdiskClean.csproj --no-restore -p:UseAppHost=false
dotnet run --project testCases/CdiskClean.ClassifierChecks/CdiskClean.ClassifierChecks.csproj
```

首次环境准备需要先还原依赖。ETW、托盘提示、图表交互、应用组即时同步及五种清理方式需在 Windows 上补充人工回归；破坏性清理仅使用专用临时测试目录。

本文交付仅新增实现文档，未修改应用代码、数据库或功能状态，也未执行上述功能验收。
